# 메타 이슈 #43 마감: 다섯 주제가 어떻게 끝났나

2026-09-15 오전에 "DS4.1F 포크에서 지금 풀어야 할 다섯 주제" 를 적었다. 하루 뒤인 2026-09-16, 그 다섯 주제가 모두 끝났다. 이 글은 각 주제가 무엇을 물었고 무엇으로 답했는지를 정리한 마감 기록이다.

**결과부터**: 다섯 주제 열두 건이 전부 닫혔고, 열린 채로 남은 것은 API 프런트엔드 배치(#29) 하나뿐이다. 오너가 최종 배포 때 하기로 정했다. 프로덕션 최종 기준선 `prod-final` 을 SGLang 대비로 환산하면 산문 94.4, 코드 94 대 97, 128K 프리필 80.1, 에이전트 작업 속도 102.3 으로 다섯 항목 모두 원 목표 범위에 들었다.

**얼마나 달라졌나**를 먼저 보면 이렇다. 왼쪽이 이슈를 쓰던 2026-09-15 오전의 값(`fork0914`), 오른쪽이 2026-09-16 최종 기준선(`prod-final`)이다.

| 주제 | 당시 | 지금 | SGLang |
| --- | ---: | ---: | ---: |
| 1 코드 디코드 c1 스텝 | 75.9 ms | **67.7 ms** | 65.4 ms |
| 2 128K 콜드 프리필 | 1,359 tok/s | **1,684 tok/s** | 2,102 tok/s |
| 3 헤드 여유 최저 | 4.64 GiB | **7.71 GiB** | 15.08 GiB |
| 4 긴 프리필 뒤 짧은 요청 | 88.9 s | **14.6 s** | 56.4 s |
| 5 운영: 부팅 뒤 첫 8K TTFT | 21.0 s | **5.3 s** | 해당 없음 |

세 번째 줄에 주의가 필요하다. 헤드 여유는 코드로 만든 차이가 아니라 시작 메모리 상태가 달라서 생긴 차이이기도 하다. 값을 그대로 성과로 읽지 않는 게 맞다.

숫자의 정본은 저장소 `.notes/` 아래 각 이슈의 `results.md` 다. 이 글의 모든 값은 거기서 다시 읽어 맞췄다. 주제별 대장은 `.notes/2026-09-16-issue-43-meta-close/plan.md` 에 있다.

## 1. 디코드 스텝 격차

같은 vLLM 코어인데 우리 스텝이 75.9 ms, T2W 기본 설정이 68.0 ms, SGLang 이 65.4 ms 였다. 스텝당 통과 토큰은 셋 다 5.82 로 같았으니 순수하게 스텝이 느린 것이었다. 세 갈래로 나눠 물었다.

| 갈래 | 당시 | 지금 |
| --- | --- | --- |
| Engram prefault 가 GPU 를 놀린다 (#37) | 스텝당 GPU 유휴 5.6 ms | 4.3 ms, 스텝 70.6 → 68.2 ms |
| 업스트림 커널 이식 (#40) | 미지 | 개선 0.3 %, 미채택 |
| 동시 2 의 증가분 (#41) | 원인 미상, +27 ms | +26.4 ms 를 전부 귀속, MoE 라우팅이 75 % |

### 원인은 콜드 NVMe 읽기였다

디코드 스텝마다 러너가 해시를 GPU 에서 내려받은 뒤 Engram 표 두 장을 **직렬로** 기다렸다. 그 표의 페이지가 디스크에서 갓 읽히는 콜드 페이지다. 샤드 상주율이 0.1 %, 표 하나가 72~96 페이지, 장치가 초당 22,000 IOPS 이니 기다림이 길 수밖에 없다. 그동안 GPU 는 스텝당 5.6 ms 를 놀았다.

고친 방법은 순서를 바꾼 것이다. 레이어 1 표만 기다리고 레이어 14 표는 포워드가 도는 동안 배경에서 채운다(`ENGRAM_DECODE_ASYNC=1`, `ENGRAM_CHUNK_RUNS=8`).

| 구성 | 코드 c1 스텝 | 산문 c1 스텝 | GPU 유휴 |
| --- | ---: | ---: | ---: |
| 기준선 | 73.3 / 70.2 / 68.4 ms | 70.3 / 70.7 ms | 5.6 ms |
| 채택 | **68.2 / 67.0 ms** | **68.5 / 67.5 ms** | **4.3 ms** |

수락 길이 5.82 는 그대로고 158K 니들도 콜드와 후속 턴 모두 정답이었다.

표 두 장을 모두 배경으로 돌리는 모드도 만들어 봤다. 코드는 64.6 ms 로 더 빠른데 산문이 3.7 ms 느려진다. 산문에서는 레이어 1 조회가 콜드 페이지를 GPU 제자리 fault 로 읽기 때문이다. 코드는 n-gram 반복 덕에 페이지가 이미 올라와 있어 fault 가 없다. 워크로드에 따라 갈리므로 옵트인으로 남겼다.

### 업스트림 커널은 값이 없었다

업스트림 vLLM PR 56562 에서 `token_to_req_indices` 커널만 떼어 이식하고 기본 꺼짐으로 게이트한 뒤 A/B 했다. 코드 c1 평균 스텝 개선이 0.304 % 였다. 채택선 2.7 % 에 한참 못 미쳐 코드를 되돌렸다. 정확성 자체는 문제가 없었다(경계 케이스 36 건 통과). **효과가 없다는 것을 확인한 것이 결과다.**

### 동시 2 의 증가분은 MoE 라우팅이다

요청이 하나에서 둘로 늘 때 우리는 27 ms 가 느는데 SGLang 은 4 ms 만 늘었다. 4 랭크 프로파일 다섯 종을 떠서 증가분 전부를 범주에 배분했다.

먼저 밝혀진 것은 **증가분이 두 값 사이를 오간다**는 점이다. 두 스트림이 같은 토큰을 디코드하면 +7.3 ms, 다른 토큰이면 +26.4 ms 다. 같은 프롬프트로 두 요청을 보내도 대개 한 스텝씩 어긋나므로 벤치에서는 큰 쪽이 잡힌다.

큰 쪽 26.4 ms 의 내역이다.

| 범주 | 증가분 | 비중 |
| --- | ---: | ---: |
| MoE 그룹 GEMM | 19.78 ms | 75 % |
| NCCL (전송 2.24, 대기 0.53) | 2.77 ms | 10 % |
| Engram 표 대기 | 2.08 ms | 8 % |
| MoE 글루 | 0.93 ms | 4 % |
| 어텐션과 인덱서 | 0.59 ms | 2 % |
| dense GEMM 과 기타 커널 | 0.47 ms | 2 % |

MoE 19.78 ms 중 15.42 ms 는 순수한 **라우팅 몫**이다. 두 토큰이 서로 다른 전문가로 가면 커널이 읽어야 할 가중치 블록이 늘어난다. 호스트 쪽은 전부 0.2 ms 미만이었다. 2026-09-14 보고서가 이 격차를 "호스트 지문" 으로 추정했는데 **그 추정은 틀렸다.**

3 ms 를 넘는 몫이 MoE 라우팅 하나뿐이고, 그것은 국소 수정으로 없앨 수 있는 성질이 아니다. 그래서 후속 이슈를 만들지 않았다.

## 2. 프리필 격차

128K 콜드 프리필이 우리 1,359 tok/s, T2W 2,034, SGLang 2,102 였다.

| 갈래 | 당시 | 지금 |
| --- | --- | --- |
| 인덱서 TP 분할 이식 (#38) | 미적용 | 채택, S128 +22 ~ 23 % |
| 프리필 커널 후속 (#17) | 병목 미상 | 청크의 26 % 가 랭크 불균형, 그 73 % 가 Engram 조회 |

지금 `prod-final` 은 S128 첫 회 1,683.7 tok/s 다. 세 회차가 1,598.9 에서 1,683.7 사이로 흔들린다. 32K 는 1,880.9 tok/s, 긴 세션 158.5K 콜드는 1,933.6 tok/s 로 SGLang 의 98 % 까지 왔다.

### 인덱서 분할이 가장 큰 한 방이었다

T2W 의 인덱서 프리필 TP 분할을 우리 트리로 옮겼다. 같은 빌드에서 게이트만 바꿔 잰 A/B 에서 **S128 이 22.41 %, 23.11 % 늘었다.** prefault 를 끄든 켜든 양쪽에서 같은 방향이었다. 32K 에서는 개선이 확인되지 않았다.

랭크마다 설정이 어긋나면 조용히 이상해지는 대신 기동을 거부하게 했다. 워커 한 대만 값을 다르게 두고 띄우니 시작 58 초 뒤 가중치 로드 전에 네 랭크가 같은 줄로 죽었고 교착은 없었다.

동일성 검증은 비트 비교로 할 수 없었다. 분할을 켜지 않아도 같은 프롬프트 재실행이 비트 단위로 같지 않기 때문이다. `top_k_per_row_prefill` 의 행 안 순서가 회차마다 달라진다. 8K 프롬프트는 첫 토큰이 거의 동점이라(logprob 차 0.3 nats 안) 판정에 쓸 수 없었고, 32K 는 네 번 중 세 번이 같은 텍스트였다. 그래서 GPU 커널 수준의 동일성으로 판정했다.

### 남은 프리필 시간은 어디 있나

128K 청크 하나가 2,224.5 ms 다. 이것을 네 랭크에서 뜯었다.

가장 큰 발견은 **청크 시간의 26 %(583 ms)가 랭크 불균형 대기**이고, 그중 **73 %(425 ms)가 Engram 조회 커널의 랭크 차**라는 것이다. 조회 시간이 랭크마다 46, 144, 328, 468 ms 로 벌어진다. 표당 페이지 수(26,000 에서 44,000)보다 훨씬 가파르게 늘고, 다음 청크 프리페치와 겹칠 때 약 두 배가 된다.

mHC tilelang 커널 세 개는 의심을 벗었다. 대역폭 하한의 1.24 ~ 1.37 배로 이미 충분히 빠르다. 하한까지 줄여도 청크당 68 ms, GEMM 패스를 융합으로 아예 없애도 66.4 ms 로 3 % 판정선(66.7 ms)에 걸린다. **여기는 손댈 값이 없다. 코드 변경 0 이다.**

Engram 조회 쪽은 레버를 만들어 봤다. 다음 청크 프리페치를 이번 스텝 조회가 GPU 에서 끝난 뒤로 미루는 것이다. 프로파일에서는 잘 들었다(랭크 3 조회 468 → 255 ms, 불균형 손실 583 → 440 ms, 청크 창 5.9 % 단축). 그런데 프로파일러를 끄고 S128 로 재니 1,733 대 1,737 tok/s 로 차이가 없었다. 되돌렸다.

여기서 방법론 문제가 드러났다. **S128 회차 편차가 1,625 에서 1,942 tok/s 로 ±8 %** 다. 매 회차 헤드에서 스왑과 직접 회수, 컴팩션이 일어난다. 이 잡음이 청크당 6 % 짜리 이득을 세 회 평균으로는 가릴 만큼 크다. 편차를 줄인 프로토콜로 다시 판정하는 일을 #44 에 파킹했다.

## 3. 이미 있는 노브 반영과 헤드 메모리 여유

노브 셋은 각각 실측 이득이 있었지만 함께 켜면 헤드 여유가 earlyoom 선 2.43 GiB 가까이 내려갈 수 있었다.

| 갈래 | 당시 | 지금 |
| --- | --- | --- |
| 노브 조합 (#36) | 개별 이득만 확인 | 두 노브 프로덕션 기본값, AG4 208.0 s |
| 헤드 여유 레버 (#31) | 오너 결정 대기 | 현상 유지로 닫음 |

세 노브를 다 켠 구성이 총시간은 가장 짧았지만 헤드 최저가 2.66 GiB 로 내려갔다. earlyoom 선에 너무 가깝다. 하나를 뺀 조합이 답이었다.

| 구성 | 에이전트 4×6 총시간 | 헤드 최저 | 158K 후속 턴 TTFT |
| --- | ---: | ---: | ---: |
| 2026-09-13 채택본 | 235.6 s | 4.33 GiB | 3.13 s |
| 세 노브 전부 | 181.7 / 176.4 s | 2.66 GiB | 1.007 s |
| **채택 (두 노브)** | **208.0 s** | **3.67 GiB** | **0.77 s** |

`SPEC_BLOCK_DROP=0` 과 `ENGRAM_RELEASE=0` 을 기본값으로 반영했다. 총시간 26 초를 포기하고 헤드 여유 1 GiB 를 샀다. 성능과 메모리 여유가 함께 서는 지점이 여기다.

헤드 여유 자체를 늘릴 레버(#31)는 오너가 현상 유지로 정해 닫았다. 신규 부팅 뒤 493K 콜드 프리필 여유가 4.6 에서 7.1 GiB 로 흔들리는 원인은 웜업 컴팩션 때 커널이 파일 회수와 anon swap-out 중 무엇을 고르느냐였고, 프로세스가 늘어난 것은 아니었다.

## 4. 긴 프리필 중 다른 요청의 대기

128K 프리필이 도는 동안 뒤에 들어온 짧은 요청이 답을 받기까지 우리 88.9 초, SGLang 56.4 초가 걸렸다. 사용자가 가장 먼저 체감하는 항목이다.

| 당시 | 지금 |
| ---: | ---: |
| 88.9 s | **14.6 s** (#34 측정 회차에서는 7.0 / 8.7 s) |

`DSPARK_SHORT_RESERVE=4096` 으로 짧은 요청 몫을 미리 떼어 놓는다. 총시간을 늘리지 않는 것이 조건이었고, 지켜졌다.

| 항목 | 기준 | 기준선 | 채택 후 |
| --- | --- | ---: | ---: |
| 짧은 요청 대기 (2 회) | 15 s 이하 | 64.50 / 68.01 s | **7.01 / 8.71 s** |
| MIXLONG 총시간 | +2 % 이내 | 265.0 s | 265.4 s (+0.15 %) |
| 에이전트 단독 총시간 | ±3 % | 205.6 s | 199.3 s (−3.1 %) |
| S128 단독 프리필 | 불변 | 1,666.3 tok/s | 1,792.2 tok/s |
| 로짓 뒤집힘 | 0 | 0 | 0 |

### 첫 시도는 효과가 0 이었다

이 작업에서 기록할 값어치가 있는 것은 **첫 부팅이 아무 효과가 없었다**는 사실이다. 예약을 4,096 토큰 걸었는데 대기가 64.96 / 66.73 초로 기준선과 똑같았다. 한 토큰도 물리지 않았다.

원인은 프리필 청크를 묶는 상한이 토큰 예산이 아니라 **입력 예산에서 드래프트 슬롯을 뺀 값**이었다는 것이다. dspark 드래프터가 요청마다 슬롯 4 개를 가져가고, 스케줄되는 요청은 입력 예산에서 새 토큰 수와 슬롯 수를 함께 뺀다. 프리필 청크가 4,092 토큰을 받아 입력 예산을 0 으로 만들면 대기 루프가 즉시 빠져나가 짧은 요청이 입장하지 못한다. 토큰 예산에서만 빼 봤자 상한을 고르는 `min()` 이 여전히 입력 예산 쪽을 골랐다.

두 예산 모두에서 자르도록 고치자 같은 부팅 조건에서 7.01 / 8.71 초가 나왔다. 이 실수를 그대로 재현하는 단위 테스트를 남겼다.

## 5. 운영

프로덕션이 사람 손 없이 서 있어야 하고, 대시보드가 사실을 보여야 하고, 디스크에 쌓인 KV 가 멀쩡한지 확인할 수단이 있어야 한다.

| 갈래 | 당시 | 지금 |
| --- | --- | --- |
| 하드닝 (#32) | 수동 기동, 웜업 없음 | 영속 유닛과 워치독, 첫 8K TTFT 21.0 → 5.3 s |
| Grafana 프리필 패널 (#33) | 복원을 프리필로 셈 | 계산 토큰 기준, 실측과 +1.2 % |
| fs 티어 CRC 검증 (#10) | 수단 없음 | 104 만 파일 전수, 불일치 0 |
| API 프런트엔드 (#29) | 열림 | 열림, 오너가 최종 배포 때로 정함 |

### 사람이 개입하지 않아도 서 있는다

네 노드의 `dsv41-serve` 를 사용자 systemd 영속 유닛으로 만들고(`Restart=always`, enabled), head 에 1 분 워치독 타이머와 부팅 웜업 요청을 붙였다.

| 시나리오 | 복구 시간 |
| --- | ---: |
| 워커 프로세스 kill | 241 s (health 응답까지 226 s) |
| 워커 재부팅 | 302 s |
| head 재부팅 | 약 4 분 |

웜업 덕에 부팅 뒤 첫 사용자 요청(8K)의 TTFT 가 21.0 초에서 5.34 초로 줄었다. 사람이 첫 요청으로 캐시를 데워 주던 일을 시스템이 한다.

검증 중에 실제 결함 두 개가 나와 그 자리에서 고쳤다. 첫째, 워커를 재부팅하면 `/mnt/kvdisk` NFS 마운트가 돌아오지 않아 랭크 3 의 디렉터리 생성이 실패하고 start limit 에 걸려 서버가 약 16 분 내려갔다. fs 티어 디렉터리를 랭크 0 에서만 만들게 고쳤다. 마운트를 부팅 때 복원할지는 오너 결정이라 #45 에 남겼다. 둘째, 생각하기 모드 요청의 첫 토큰이 다른 필드로 와서 웜업이 실패로 기록하던 것을 고쳤다.

### 대시보드가 프리필을 잘못 세고 있었다

Grafana 프리필 패널이 KV 복원까지 프리필 토큰으로 세고 있었다. 분자를 실제 계산 토큰 기준으로 바꿨다. 재기동도 설정 변경도 없었고 **엔진 코드 변경 0** 이다.

| 확인 항목 | 결과 |
| --- | --- |
| 요청당 프리필 속도 패널 대 실측 | 1,867.3 대 1,845.3 tok/s (+1.2 %) |
| 계산 토큰 증가 대 보낸 프롬프트 토큰 | 95,826 = 95,826 |
| 정지 식 | 12 샘플 모두 0 |
| 493K 복원 때 프리필 패널 | 0 에서 2 tok/s |
| 493K 복원 때 복원 패널 | +492,928 토큰, 8,215 에서 16,430 tok/s |

첫 배포 때 붙였던 설명도 틀린 것이어서 고쳤다. "복원 요청은 복원 시간이 분모에 들어가 낮게 보인다" 고 적었는데, 실제로는 복원 대기가 큐 시간으로 잡혀 분모에 거의 들어가지 않는다. 493K 복원 한 번에서 TTFT 8.6 초 중 0.49 초였다.

### 디스크의 KV 104 만 파일이 전부 멀쩡했다

fs 티어 검증 스크립트를 만들어 라이브 저장소를 전수 검사했다.

```text
kvfs_verify: root=/mnt/kvdisk/kv/dsv41 files=1045649 ok=1045649
             size_mismatch=0 no_xattr=0 crc_mismatch=0 unknown=0
             vanished=0 skipped_tmp=0 deleted=0 secs=590.283 mib_s=169.91
```

**1,045,649 파일 전부 일치, 불일치 0, 삭제 0** 이다. 약 10 분에 초당 170 MiB 로 읽었다. 스크립트는 기본이 보고 모드이고, 서버 상태가 불확실하면 삭제를 거부하며, 삭제 직전에 분류와 파일 identity 를 다시 본다. 실제 삭제는 서버가 내려간 창에서만 한다.

## 최종 수치

프로덕션 최종 기준선 `prod-final` 을 SGLang=100 으로 환산한 값이다.

| 항목 | prod-final | SGLang | 점수 | 원 목표 |
| --- | ---: | ---: | ---: | --- |
| 산문 c1 | 45.23 tok/s | 47.93 | **94.37** | 100 근처 |
| 코드 c1 | 85.95 tok/s | 89.03 | **96.54** | 96 ~ 100 |
| 코드 c4 합산 | 194.25 tok/s | 238.22 | **81.54** | 76 이상 |
| 128K 프리필 첫 회 | 1,683.70 tok/s | 2,102.10 | **80.10** | 75 ~ 97 |
| 에이전트 작업 속도 | 191.85 s | 196.25 s | **102.29** | 93 ~ 100 |

**다섯 항목 모두 목표 범위에 들었다.** 128K 프리필은 첫 회 기준이고 세 회 평균을 쓰면 77.95 다. 두 기준을 섞지 않는다.

## 오너에게 남기는 것

결정이나 처분이 필요해 그대로 둔 것들이다. 이 글에서 새로 판단하지 않았다.

| 항목 | 내용 |
| --- | --- |
| 옛 kvfs 저장소 | `~/dsv41-prep/kvfs`, 37 GiB, 네 노드 모두 |
| f323 도커 이미지 | `dsv41-4x-spark:local` 과 dangling `<none>` `171c04994a8d`, 각 33.2 GB |
| 워커 NFS fstab (#45) | 워커 재부팅 때 `/mnt/kvdisk` 를 자동 복원할지 |
| API 프런트엔드 (#29) | 최종 배포 시점 |
| Engram 조회 게이트 (#44) | 편차를 줄인 프로토콜로 재판정할지 |

보류 상태인 세 건(EP=1 옵션 #16, KV 풀 블록 패딩 회수 #21, fs 티어 I/O 워커 이전 #23)은 그대로 둔다.

## 이번 마감에서 하지 않은 것

엔진과 배포 코드, `dsv41.env`, 오버라이드 파일, 노드 트리를 건드리지 않았다. 서버 재기동, 벤치, 테스트 실행이 없다. 노드 sync 도 하지 않았다. 변경이 `.notes` 디렉터리와 `.gitignore` 한 줄뿐이고, 노드는 `.notes` 를 받지 않는 것이 규칙이다. 삭제 명령도 없다.

이슈 #43 은 본문의 현재 상태 절만 갱신하고 열어 둔다. 닫는 것은 매니저가 확인한 뒤에 한다.
